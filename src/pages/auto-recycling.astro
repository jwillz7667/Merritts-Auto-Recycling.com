---
import ServicePage from '@/components/ServicePage.astro';
import { services } from '@/data/site';

const service = services.find((item) => item.slug === 'auto-recycling');
if (!service) throw new Error('Auto-recycling content is missing.');
---

<ServicePage service={service} />
