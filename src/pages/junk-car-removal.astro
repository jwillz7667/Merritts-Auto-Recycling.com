---
import ServicePage from '@/components/ServicePage.astro';
import { services } from '@/data/site';

const service = services.find((item) => item.slug === 'junk-car-removal');
if (!service) throw new Error('Junk-car-removal content is missing.');
---

<ServicePage service={service} />
