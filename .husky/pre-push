#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Heavier checks before code leaves the developer's machine. These mirror
# what CI runs but catch issues earlier so the pipeline stays green.
echo "→ typecheck"
npm run typecheck --silent

echo "→ schema validation"
npm run validate --silent
